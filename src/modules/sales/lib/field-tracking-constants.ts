/** How often a rep in field mode sends a location ping while tracking is on. */
export const FIELD_LOCATION_PING_INTERVAL_MS = 5 * 60 * 1000;

/** How often the manager live map refreshes rep locations from the server. */
export const FIELD_REP_LOCATIONS_POLL_MS = 5 * 60 * 1000;

/** Trail polyline refresh while a rep is selected on the live map. */
export const FIELD_TRAIL_POLL_MS = 5 * 60 * 1000;
